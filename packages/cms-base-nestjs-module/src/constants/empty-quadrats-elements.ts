import { PARAGRAPH_TYPE, QuadratsElement } from '@quadrats/core';

export const EMPTY_QUADRATS_ELEMENTS = [
  {
    type: PARAGRAPH_TYPE,
    children: [
      {
        text: '',
      },
    ],
  },
] as QuadratsElement[];
