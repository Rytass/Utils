export interface ImageDetailURL {
  id: string; // file id
  preload: string; // for blur preload url
  thumbnails: string; // small size url
  public: string; // normal size url
  full: string; // large size url
}
